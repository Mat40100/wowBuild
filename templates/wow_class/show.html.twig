{% extends 'base.html.twig' %}

{% block title %}WowClass{% endblock %}

{% block body %}
    <h1>WowClass</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wow_class.id }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ wow_class.img }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ wow_class.name }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ wow_class.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_wowclass_index') }}">back to list</a>

    <a href="{{ path('app_wowclass_show', {'id': wow_class.id}) }}">edit</a>

    {{ include('wow_class/_delete_form.html.twig') }}
{% endblock %}
